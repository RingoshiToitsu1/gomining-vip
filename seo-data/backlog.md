# SEO backlog — gmt-optimizer.com

Ranked. Top item is the default action when the data says nothing new.
Seeded 2026-07-26 from the 2026-07-23 analysis.

1. **Give the three page-9 guides exact-match anchors in `index.html:384–388`** —
   229 imp / 3 clicks at position ~9; the ROI anchor omits "calculator", the exact
   word in both its target queries. *(added 2026-07-26 — this is today's action)*

2. **Raise the guide list out of the bottom of the page.** It sits at line 384 of
   491, below the FAQ; the three target pages have no other in-content homepage
   link. Prominence is the other half of the equity lever. *(added 2026-07-26)*

3. **Verify the snapshot pull is actually refreshing.** `latest.json` was
   byte-identical to `gsc-2026-07-23.json` on the 2026-07-26 run. One repeat is
   explicable, two means `scripts/gsc-snapshot.py` is reporting stale data and
   every downstream conclusion is suspect. *(added 2026-07-26)*

4. **Resolve homepage vs `/gomining-roi-calculator` cannibalisation on
   "gomining calculator".** Homepage H2 (`index.html:373`) targets the same head
   term; homepage ranks 23.5, the ROI page 15.75. Pick one owner. Below the
   impression bar (10 imp) — do it when the term grows or while editing anyway.
   *(added 2026-07-26)*

5. **Decide whether `gomining-worth-it-now.html` should keep canonicalising to
   `/is-gomining-worth-it`.** It cannot rank independently while it does, despite
   position 3.14. Confirm this is deliberate. *(added 2026-07-26)*

6. **Plan non-branded coverage.** Non-brand clicks are ~4 in 28 days; 12 of 22
   come from one navigational query. The branded `gomining-*` cluster is near
   fully harvested at this size. Targets: "cloud mining worth it", "bitcoin mining
   without hardware". Currently zero impression data because no page targets them.
   A project, not a day's task — break it into one page before starting.
   *(added 2026-07-26)*

## Explicitly not doing

- **More promo/coupon pages, or any work on `/gomining-promo-code.html`.** Eleven
  near-duplicate variants already rank 25–54 on one page at position 30.9. That
  is an authority ceiling, not a coverage gap; more pages land in the same place.
  Revisit only after the page-9 cluster moves. *(noted 2026-07-26)*
- **Rewriting meta descriptions on the page-9 guides.** At position ~9 their 1.3%
  CTR is roughly what the position earns. Snippet work cannot fix a ranking
  constraint. *(noted 2026-07-26)*
